import React from 'react';
import Hero from '../../components/hero/Hero';

const PersonalSpace = () => {
    return (
        <section>
            <Hero />
        </section>
    );
};

export default PersonalSpace;
